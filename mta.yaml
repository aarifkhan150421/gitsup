_schema-version: "3.2"
ID: gitsup
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: gitsup-destination-content
  type: com.sap.application.content
  requires:
  - name: gitsup-destination-service
    parameters:
      content-target: true
  - name: gitsup-repo-host
    parameters:
      service-key:
        name: gitsup-repo-host-key
  - name: gitsup-uaa
    parameters:
      service-key:
        name: gitsup-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: gitsup_html_repo_host
          ServiceInstanceName: gitsup-html5-srv
          ServiceKeyName: gitsup-repo-host-key
          sap.cloud.service: gitsup
        - Authentication: OAuth2UserTokenExchange
          Name: gitsup_uaa
          ServiceInstanceName: gitsup-xsuaa-srv
          ServiceKeyName: gitsup-uaa-key
          sap.cloud.service: gitsup
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: gitsup-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: gitsup-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - gitsup.zip
      name: gitsup
      target-path: resources/
- name: gitsup
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: gitsup-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: gitsup-destination-service
    service-plan: lite
- name: gitsup-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: gitsup-xsuaa-srv
    service-plan: application
- name: gitsup-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: gitsup-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
